# The Legend of Heroes: Trails through Daybreak (Kuro no Kiseki)

`CUSA27774.savepatch`

### 1. Fix custom CRC32 (Required)

Target File: `user.dat`

```
import loh_trails
import ubinascii
trails_crc = loh_trails.loh_trails_crc32(savedata, 12)
print("New CRC:", ubinascii.hexlify(trails_crc))
savedata[8:12] = trails_crc
```

